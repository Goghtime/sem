# Semaphore connection
SEMAPHORE_URL=https://your-semaphore-instance.example.com
SEMAPHORE_API_TOKEN=your_token_here
SEMAPHORE_DEFAULT_PROJECT=1

# Local SSH key paths (name=path, comma-separated)
# SEMAPHORE_KEY_MAP="key_name=~/.ssh/key_file,other_key=~/.ssh/other"
